RDM Power Automate Flow Validation (Contract & Session Status)


Validation completed for the Contract RDM Power Automate flow.
Verified the SharePoint → Dataflow → silver_rdm_contract flow and confirmed the mapping is correct. Existing record matching was validated using contr_src_name, contr_src_id, and contr_src_sys_inst_id. No duplicates were found, and the flow is ready to be enabled.




Validation completed for the Session Status RDM Power Automate flow.
Verified the SharePoint → Dataflow → silver_rdm_session_status flow and confirmed the mapping is correct. Existing record matching is based on session_status_src_id, and no duplicates were found in either silver_rdm_session_status or silver_rdm_session_status_add. The flow is ready to be enabled.