Updated the WIP `form_ans_date_time` implementation as per the revised definition. The field now uses `parsed_form_ans_date` cast as a timestamp, resulting in the answer date with the time defaulted to `00:00:00` instead of NULL.
